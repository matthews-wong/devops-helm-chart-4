apiVersion: v1
kind: Service
metadata:
  name: {{ include "echo-web.fullname" . }}
  labels:
    {{- include "echo-web.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
  selector:
    {{- include "echo-web.selectorLabels" . | nindent 4 }}
