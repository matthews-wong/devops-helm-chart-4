apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "echo-web.fullname" . }}
  labels:
    {{- include "echo-web.labels" . | nindent 4 }}
data:
  index.html: |
    {{- .Values.indexHtml | nindent 4 }}
